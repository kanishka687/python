import heapq

class MedianFinder:
    def __init__(self):
        # max_heap stores the smaller half (negative values for max-heap behavior)
        self.small = [] 
        # min_heap stores the larger half
        self.large = [] 

    def addNum(self, num: int) -> None:
        # Push to small (max-heap), then move max of small to large (min-heap)
        heapq.heappush(self.small, -num)
        heapq.heappush(self.large, -heapq.heappop(self.small))
        
        # Maintain balance: small can have at most 1 more element than large
        if len(self.large) > len(self.small):
            heapq.heappush(self.small, -heapq.heappop(self.large))

    def findMedian(self) -> float:
        if len(self.small) > len(self.large):
            return float(-self.small[0])
        return (-self.small[0] + self.large[0]) / 2.0
